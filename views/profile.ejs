<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - Student Helper</title>
    <link rel="stylesheet" href="/style.css">

    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: Arial, sans-serif;
        }

        .profcon {
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            width: 400px;
            text-align: center;
            position: relative;
        }

        .exit {
            position: absolute;
            top: 15px;
            right: 15px;
            background: #e74c3c;
            border: none;
            color: white;
            padding: 10px 15px;
            border-radius: 8px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s;
        }

        .exit:hover {
            background: #c0392b;
            transform: scale(1.05);
        }

        .picture {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            overflow: hidden;
            border: 4px solid #667eea;
            margin: 0 auto 20px;
            position: relative;
        }

        .picture img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .ok {
            position: relative;
            width: fit-content;
            margin: auto;
        }

        .chang {
            position: absolute;
            bottom: 5px;
            right: 5px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            font-size: 18px;
            font-weight: bold;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: 0.3s;
            box-shadow: 0 3px 8px rgba(0, 0, 0, 0.3);
        }

        .chang:hover {
            transform: scale(1.1);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

        

        form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        form div {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
        }

        label {
            font-weight: bold;
            color: #444;
            margin-bottom: 5px;
        }

        input {
            padding: 12px;
            border-radius: 10px;
            border: 2px solid #ddd;
            font-size: 16px;
            width: 100%;
            outline: none;
            transition: all 0.3s;
        }

        input:focus {
            border-color: #667eea;
            box-shadow: 0 0 10px rgba(102, 126, 234, 0.3);
        }

        

        .save {
            margin-top: 10px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 12px;
            border: none;
            border-radius: 30px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: 300ms;
        }

        .save:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
        }

       

        .logout {
            display: block;
            width: 60%;
            margin: 25px auto 0 auto;
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a6f 100%);
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 30px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .logout:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(255, 107, 107, 0.4);
            background: red;
        }
    </style>
</head>
<body>
    <div class="profcon">
        <button class="exit" onclick="window.location.href='/home'">Exit ✖</button>

        <div class="ok">
            <div class="picture">
                <img id="preview" src="<%=user.profile %>" alt="Profile Picture">
            </div>

            <label for="prof" class="chang hello">+</label>
            <input id="prof" style="display: none;">
        </div>

        <form method="post" action="/profile/update">
            
            <div>
                <label for="name">Full Name</label>
                <input name="fullname" type="text" id="name" value="<%=user.name %>" placeholder="Enter your name">
            </div>

            <div>
                <label for="email">Gmail</label>
                <input name="email" type="email" id="email" value="<%=user.email %>" placeholder="Enter your Gmail">
            </div>

            <div>
                <label for="password">Password</label>
                <input name="password" type="password" value="<%=user.password %>" id="password" placeholder="Enter new password">
            </div>

            <button type="submit" class="save">Save Changes</button>
        </form>

        <form action="/logout">
            <button class="logout" >Logout</button>
        </form>

    </div>

    <script>
        document.getElementById("prof").addEventListener("change", function(e) {
            const file = e.target.files[0];
            if (!file) return;
            const reader = new FileReader();
            reader.onload = function(event) {
                document.getElementById("preview").src = event.target.result;
            };
            reader.readAsDataURL(file);
        });
    </script>
</body>
</html>
